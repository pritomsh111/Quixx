let digits = [1, 2, 3];

let length = digits.length - 1;

let lastDigit = digits[length] + 1;

if (lastDigit !== 0) {
    digits[length] = lastDigit;
}
else {
    for (i = length - 1; i >= 0; i--) {

    }
}